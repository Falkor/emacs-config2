;; ==============================================================
;; Autopair: Automagically pair braces and quotes like TextMate
;; see http://code.google.com/p/autopair/ or
;; http://www.emacswiki.org/emacs/AutoPairs
;; ==============================================================
;;(require 'autopair)

;; no longer required since Emacs-24 comes with a new simple global minor mode
;; electric-pair-mode which uses the new post-self-insert-hook.  



;; (use-package autopair
;;   :commands (autopair-global-mode)
;;   :config
;;   (progn
;; 	(autopair-global-mode) ;; enable autopair in all buffers
;; 	(setq autopair-autowrap t)))
;;   ;; :bind ("C-j" . autopair-newline))

